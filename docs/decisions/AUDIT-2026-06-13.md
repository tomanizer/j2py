# Audit refresh — 2026-06-13

> **Superseded in part by** [AUDIT-2026-06-15.md](AUDIT-2026-06-15.md) (maturity assessment,
> multi-library scorecard, correctness-evidence gap, and P0/P1 follow-up).
> This file remains as the 2026-06-13 rule-layer breadth snapshot.

> **Note:** This snapshot predates the multi-library corpus documentation refresh (#201).
> Spring-focused wording below reflects the measurement presets active at audit time.
> See [docs/CORPUS_SCOREBOARD.md](../CORPUS_SCOREBOARD.md) for the current preset table.
> Current target-test state lives in [docs/TRANSLATION_TARGETS.md](../TRANSLATION_TARGETS.md);
> the `FUTURE_TARGETS` counts below are historical snapshot results.

Reference: [#38](https://github.com/tomanizer/j2py/issues/38) (deterministic rule-layer roadmap — **complete**; ongoing work tracked in [#134](https://github.com/tomanizer/j2py/issues/134))

Current baseline commit: `589eadb` (`main`, 2026-06-13) — includes #87 (`get(...)` typing), P0 fixes (#108–#111), and import-tracking refactor (#146).

Initial audit snapshot: `d01061d` (streams epic landed; dense baseline improvement pending).

## Executive summary

The deterministic rule layer has reached **near-complete coverage** on the preferred Spring measurement presets. All curated construct mini-corpus files are graduated; `FUTURE_TARGETS` is empty. Remaining gaps are narrow, file-specific rule-layer items (overload dispatch, two broad-corpus one-offs) — not architecture.

**Hygiene items from the initial audit are done:** dense baseline committed at 100% average coverage; `make test-targets` succeeds when `FUTURE_TARGETS` is empty.

## Quality gates

| Command | Result |
|---------|--------|
| `make check` | **396 passed**, 16 deselected (`live_llm`, `behavior`) |
| `make test-targets` | **0 selected** (empty `FUTURE_TARGETS`); exits 0 |
| `make corpus-spring-dense-check` | **No regressions** vs committed baseline |
| `make corpus-spring-broad` (150 files) | See corpus table below |

## Corpus metrics

### Dense preset (`make corpus-spring-dense-check`)

100 files scanned, 43 in coverage metrics (density + constructs sampling).

| Metric | Committed baseline | Current | Δ |
|--------|-------------------|---------|---|
| Parse success | 100.00% | 100.00% | — |
| Syntax success | 98.00% | 98.00% | — |
| Average coverage | **100.00%** | **100.00%** | — |
| Full-coverage files | 43 | 43 | — |
| Files with unhandled | 0 | 0 | — |
| Below 80% threshold | 0 | 0 | — |

Baseline file: `tests/fixtures/corpus/spring-dense-baseline.json` (committed; no update pending).

### Broad preset (`make corpus-spring-broad`)

150 files, `spring-context` module + constructs, density strategy.

| Metric | Value |
|--------|-------|
| Parse success | 100.00% |
| Syntax success | 99.33% |
| Average coverage | **99.71%** |
| Full-coverage files | 92 / 94 |
| Files with unhandled | **2** |
| Below 80% threshold | 0 |

Top unhandled reasons (2 total):

```text
switch expression without default: 1
equals invocation with unexpected argument count: 1
```

Affected files:

- `AsyncConfigurationSelector.java` — switch expression without default (P3 one-off)
- `SimpleValueWrapper.java` — `equals` argument count (P3 one-off)

**Resolved since initial audit:** `ProfileCondition.java` ambiguous `get(...)` — fixed in #87 / PR #98.

### Broad sample (450 files, reference only)

| Metric | Value |
|--------|-------|
| Average coverage | 99.55% |
| Files with unhandled | 13 |
| Dominant remaining gap | overload dispatch (#88) |

Top unhandled reasons (450-file run):

```text
overloaded method … requires manual dispatch: 15
switch expression without default: 2
equals invocation with unexpected argument count: 2
unknown static import …: 3
anonymous class requires local helper scope: 1
enum constant class body requires manual translation: 1
```

## Construct mini-corpus

At this audit snapshot, all ten then-existing files under
`tests/fixtures/corpus/constructs/` were **graduated** (run in `make check` via
`test_graduated_corpus_construct_translates_cleanly`). See
[`docs/TRANSLATION_TARGETS.md`](../TRANSLATION_TARGETS.md) for the current construct
list.

`AdvancedEnum`, `AdvancedStreams`, `AnonymousAndInner`, `ComplexRecords`,
`InterfaceDefaults`, `SealedClasses`, `SuperMethodCalls`, `SwitchFallthrough`,
`TextBlocks`, `VarKeyword`.

`FUTURE_TARGETS` is **empty** — no strict xfail contracts remain.

## Landed since initial audit (`d01061d`)

| PR | Issue | Summary |
|----|-------|---------|
| #98 | #87 | Resolve ambiguous `get(...)` via map-like type inference |
| #97 | #38 | Initial audit doc refresh |
| #94 | #92 | `flatMap(List::stream)` → nested comprehensions |
| #95 | #93, #75 | `groupingBy` + `mapping(identity, toList())`; graduate `AdvancedStreams` |
| #135–#146 | #108–#119 | P0 fixes, module split, static imports, LLM context, import tracking |

## Open work (handoff to #134)

| Issue | Priority | Rationale |
|-------|----------|-----------|
| [#88](https://github.com/tomanizer/j2py/issues/88) | **P1** (#131) | Safe overload dispatch — dominant gap on 450-file sample |
| — | P3 | Switch expression without default (1–2 files) |
| — | P3 | `equals` unexpected argument count (1–2 files) |
| [#134](https://github.com/tomanizer/j2py/issues/134) | P0–P3 | Professional-quality audit buckets (includes #130–#133) |

## LLM layer

`main` includes validation retry with `previous_python` feedback, sibling signature context (#145), and prompt caching (#141). Rule-layer coverage on dense/broad Spring samples is high enough that LLM work should focus on **partial-coverage files** and structural verification, not replacing deterministic rewrites.

## Verification commands

```bash
make check
make test-targets
make corpus-spring-dense-check
make corpus-spring-broad
uv run j2py translate tests/fixtures/java/HelloWorld.java --no-llm --no-validate --dry-run
```

Use `--repo /path/to/.corpus/spring-framework` when the Spring clone lives outside the worktree checkout.

## Addendum — multi-library baselines (2026-06-15, #205)

At this snapshot the committed corpus baselines cover five library checkouts plus the
historical `spring-lexical` sample. Run `make corpus-hotspots` for live metrics.

| Preset | Avg coverage | Syntax OK | Unhandled files |
|--------|-------------|-----------|-----------------|
| `spring-dense` | 100% | 100% | 0/100 |
| `commons-lang-dense` | ~100% | 100% | 1/100 |
| `jackson-dense` | ~99.5% | 99% | 6/100 |
| `caffeine-dense` | ~99.6% | 97% | 8/36 |
| `guava-dense` | ~98% | 94% | 14/100 |
| `spring-lexical` | ~100% | 100% | 7/100 |

`spring-broad` remains exploratory with no committed baseline. CI runs `spring-dense`
comparison plus the hotspot scorecard over all committed baselines.
