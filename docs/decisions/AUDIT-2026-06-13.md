# Audit refresh — 2026-06-13

Reference: [#38](https://github.com/tomanizer/j2py/issues/38)

Baseline commit: `d01061d` (`main`, 2026-06-13) — merges #92–#95 (streams epic).

## Executive summary

The deterministic rule layer has reached **near-complete coverage** on the preferred
Spring measurement presets. All curated construct mini-corpus files are graduated;
`FUTURE_TARGETS` is empty. Remaining work is narrow, file-specific rule-layer gaps and
measurement hygiene — not architecture.

## Quality gates

| Command | Result |
|---------|--------|
| `make check` | **285 passed**, 15 deselected (`live_llm`, `behavior`) |
| `make test-targets` | **0 tests** collected (no `FUTURE_TARGETS`; pytest exits 5 — hygiene follow-up) |
| `make corpus-spring-dense-check` | **Improvements**, no regressions vs baseline |
| `make corpus-spring-broad` | See corpus table below |

## Corpus metrics

### Dense preset (`make corpus-spring-dense-check`)

100 files scanned, 43 in coverage metrics (density + constructs sampling).

| Metric | Baseline | Current | Δ |
|--------|----------|---------|---|
| Parse success | 100.00% | 100.00% | — |
| Syntax success | 98.00% | 98.00% | — |
| Average coverage | 99.69% | **100.00%** | +0.31% |
| Full-coverage files | 42 | **43** | +1 |
| Files with unhandled | 1 | **0** | −1 |
| Below 80% threshold | 0 | 0 | — |

**Action:** run `make corpus-spring-dense-update-baseline` to commit the improvement.

### Broad preset (`make corpus-spring-broad`)

150 files, `spring-context` module + constructs, density strategy.

| Metric | Value |
|--------|-------|
| Parse success | 100.00% |
| Syntax success | 99.33% |
| Average coverage | **99.61%** |
| Full-coverage files | 91 / 94 |
| Files with unhandled | **3** |
| Below 80% threshold | 0 |

Top unhandled reasons (3 total):

```text
ambiguous get invocation requires receiver collection type: 1
switch expression without default: 1
equals invocation with unexpected argument count: 1
```

Affected files:

- `ProfileCondition.java` — ambiguous `get(...)` (#87)
- `AsyncConfigurationSelector.java` — switch expression without default
- `SimpleValueWrapper.java` — equals argument count

## Construct mini-corpus

All 10 files under `tests/fixtures/corpus/constructs/` are **graduated** (run in
`make check` via `test_graduated_corpus_construct_translates_cleanly`):

`AdvancedEnum`, `AdvancedStreams`, `AnonymousAndInner`, `ComplexRecords`,
`InterfaceDefaults`, `SealedClasses`, `SuperMethodCalls`, `SwitchFallthrough`,
`TextBlocks`, `VarKeyword`.

`FUTURE_TARGETS` is **empty** — no strict xfail contracts remain.

## Recently landed (since 2026-06-11 audit)

| PR | Issue | Summary |
|----|-------|---------|
| #94 | #92 | `flatMap(List::stream)` → nested comprehensions |
| #95 | #93, #75 | `groupingBy` + `mapping(identity, toList())`; graduate `AdvancedStreams` |
| #90 | #89 | Annotation element defaults / `@AliasFor` |
| #86 | #83 | `@interface` → dataclass metadata |
| #85 | #82 | `super.method(...)` receiver |
| #84 | #81 | Traditional `for` without update clause |
| #79 | #74 | Anonymous class instance fields |

## Open roadmap

| Issue | Priority | Rationale |
|-------|----------|-----------|
| [#87](https://github.com/tomanizer/j2py/issues/87) | **P1** | Type inference for `get(...)` — highest signal on broad corpus |
| [#88](https://github.com/tomanizer/j2py/issues/88) | **P2** | Safe overload dispatch — higher risk; split by pattern |
| — | P3 | Switch expression without default (1 file in broad sample) |
| — | P3 | `equals` unexpected argument count (1 file) |
| — | Hygiene | `make test-targets` with empty `FUTURE_TARGETS`; update dense baseline |

## LLM layer

`main` includes validation retry with `previous_python` feedback. Rule-layer coverage
on dense/broad Spring samples is high enough that LLM work should focus on **partial-coverage
files** and structural verification, not replacing deterministic rewrites.

## Verification commands

```bash
make check
make test-targets          # expect 0 collected until next FUTURE_TARGET added
make corpus-spring-dense-check
make corpus-spring-broad
uv run j2py translate tests/fixtures/java/HelloWorld.java --no-llm --no-validate --dry-run
```
